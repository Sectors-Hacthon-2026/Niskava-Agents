[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "niskava-engine"
version = "0.1.0"
description = "Niskava Autonomous Financial OSINT & Quant Engine"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
